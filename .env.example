# Supabase project credentials. With these unset the app serves fixture data,
# so every screen still renders in local development.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role key is only read by scripts/seed.ts. Never expose it to the client.
SUPABASE_SERVICE_ROLE_KEY=
